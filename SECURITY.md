# 安全政策

Comic Plus 不自建账号系统，不收集支付信息或设备标识。用户主动登录 JM 官方账号时，账号密码只用于一次官方登录请求，不会写入本地；成功后的短期 AVS 使用 Android Keystore 加密保存。设置、续读进度、缓存和下载均保存在应用私有目录；Android 清单禁止明文流量、云备份和设备迁移备份。

源码中包含兼容上游只读协议所需的固定协议常量、请求头和公开域名。这些值可从客户端实现中观察到，不是项目凭据，也不提供仓库或发布签名的访问权限。

报告安全问题时，请提供受影响版本、Android 版本、复现步骤和影响范围。不要在公开内容中附带签名配置、密钥、Cookie、下载内容、用户搜索记录或其他个人数据。

仓库托管平台支持私密漏洞报告时，请优先使用该渠道；一般缺陷可以通过 Issue 报告。

提交前请确认没有加入 `android/keystore.properties`、密钥文件、`local.properties`、个人绝对路径、APK/AAB、构建目录、日志或用户数据。
